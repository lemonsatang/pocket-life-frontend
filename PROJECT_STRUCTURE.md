# 📊 Pocket Life Frontend - 프로젝트 실행 구조도

## 🎯 전체 실행 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                        index.html                             │
│                    (React 앱 진입점)                          │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                      main.jsx                                │
│  • Global.css 로드                                           │
│  • React StrictMode 활성화                                   │
│  • App 컴포넌트를 #root에 마운트                             │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                      App.jsx                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [인증 상태 체크]                                     │   │
│  │  const [authed, setAuthed] = useState(true)          │   │
│  └─────────────────────────────────────────────────────┘   │
│                        │                                    │
│        ┌───────────────┴───────────────┐                   │
│        │                               │                    │
│        ▼                               ▼                    │
│  ┌─────────────┐              ┌─────────────────┐          │
│  │ 미인증 상태  │              │   인증 상태      │          │
│  │ (!authed)   │              │   (authed)      │          │
│  └─────────────┘              └─────────────────┘          │
│        │                               │                    │
│        ▼                               ▼                    │
│  ┌─────────────────────────┐  ┌──────────────────────────┐  │
│  │  Auth 화면              │  │  BrowserRouter          │  │
│  │  • LoginPage           │  │  ┌────────────────────┐ │  │
│  │  • SignupPage          │  │  │ Header            │ │  │
│  │  • authBg 스타일       │  │  │ (공통 네비게이션)  │ │  │
│  └─────────────────────────┘  │  ├────────────────────┤ │  │
│                                │  │ Routes             │ │  │
│                                │  │  • / → Home        │ │  │
│                                │  │  • /meal → MealPage│ │  │
│                                │  │  • /cart → Cart    │ │  │
│                                │  │  • /schedule       │ │  │
│                                │  │  • /ledger         │ │  │
│                                │  └────────────────────┘ │  │
│                                │  │ Footer              │ │  │
│                                │  │ (공통 푸터)         │ │  │
│                                └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 📁 컴포넌트 계층 구조

### 1️⃣ 인증 상태 (미인증)

```
App.jsx (authed = false)
└── authBg (Auth.css)
    └── authWrap
        ├── brand: "Pocket Life"
        └── auth-card
            └── panel
                ├── LoginPage (view === "login")
                │   ├── LoginPage.css
                │   └── Modal (에러 표시)
                │       └── Modal.css
                │
                └── SignupPage (view === "signup")
                    ├── SignupPage.css
                    └── Modal (에러 표시)
                        └── Modal.css
```

### 2️⃣ 메인 애플리케이션 (인증 완료)

```
App.jsx (authed = true)
└── BrowserRouter
    └── app-layout (Common.css)
        ├── Header (Layout/Header.jsx)
        │   ├── Header.css
        │   ├── 네비게이션 탭 (대시보드, 식단, 장바구니, 일정, 가계부)
        │   └── 로그아웃 버튼
        │
        ├── main-content (Common.css)
        │   └── Routes
        │       ├── / → Home
        │       ├── /meal → MealPage
        │       ├── /cart → Cart
        │       ├── /schedule → PlaceholderPage
        │       └── /ledger → PlaceholderPage
        │
        └── Footer (Layout/Footer.jsx)
            └── Footer.css
```

## 🏠 페이지별 상세 구조

### 📊 Home 페이지 (/)

```
Home.jsx
├── Home.css
├── DatePicker (react-datepicker)
│   └── DatePicker.css (전역)
│
└── DashboardCard (4개)
    ├── DashboardCard.css
    ├── 일정 카드 (📅)
    │   └── linkTo: /schedule
    ├── 식단 카드 (🍚)
    │   ├── totalCalories 계산
    │   └── linkTo: /meal
    ├── 장바구니 카드 (🛍️)
    │   ├── hasUnconfirmedItems 체크
    │   └── linkTo: /cart
    └── 가계부 카드 (💰)
        ├── income/expense 표시
        └── linkTo: /ledger

[API 호출]
├── GET /api/meals?date=YYYY-MM-DD
├── GET /api/cart?date=YYYY-MM-DD
├── GET /api/todo?userId=testUser&date=YYYY-MM-DD
└── GET /api/tx?userId=testUser&date=YYYY-MM-DD
```

### 🍚 MealPage (/meal)

```
MealPage.jsx
├── MealPage.css
├── useMealData (hooks/useMealData.js)
│   └── API: localhost:8080/api/meals
│
├── DatePicker (react-datepicker)
│   └── 날짜 선택 및 네비게이션
│
├── MealTypeButtons (아침/점심/저녁/간식)
│
├── InputGroup
│   ├── meal-food-input (음식명)
│   ├── meal-calorie-input (칼로리)
│   └── 추가 버튼
│
├── MealList (components/Meal/MealList/)
│   ├── MealList.css
│   ├── 편집 모드
│   │   ├── meal-list-edit-input
│   │   └── meal-list-edit-calorie-input
│   └── 표시 모드
│       ├── meal-list-meal-type
│       ├── meal-list-meal-text
│       └── meal-list-meal-calories
│
└── MealStats (components/Meal/MealStats/)
    ├── MealStats.css
    ├── 총 칼로리 표시
    ├── 진행률 바
    └── 추천 식단 카드
```

### 🛍️ Cart 페이지 (/cart)

```
Cart.jsx
└── CartList (components/Cart/CartList.jsx)
    └── CartView (components/Cart/CartView/)
        ├── CartView.css
        ├── CartMainSection
        │   ├── CartMainSection.css
        │   └── CartItem (반복)
        │       ├── CartItem.css
        │       ├── 애니메이션 효과
        │       └── 구매 완료 체크박스
        │
        └── CartFavSection
            ├── CartFavSection.css
            └── 즐겨찾기 아이템 목록
```

## 🎨 스타일 구조

```
styles/
├── Global.css (main.jsx에서 로드)
│   ├── @import 폰트 (Jua)
│   ├── CSS Reset
│   └── body 기본 스타일
│
├── Common.css (App.jsx에서 로드)
│   ├── .app-layout
│   ├── .main-content
│   ├── .pixel-card
│   ├── .pixel-input
│   ├── .pixel-btn
│   └── .item-row
│
├── Auth.css (App.jsx에서 로드)
│   ├── .authBg
│   ├── .authWrap
│   └── .auth-card
│
└── DatePicker.css (App.jsx에서 로드)
    └── react-datepicker 커스텀 스타일
```

## 🔄 데이터 흐름

### 인증 흐름

```
1. App.jsx 마운트
   ↓
2. localStorage에서 mock_token 확인
   ↓
3. authed 상태 결정
   ↓
4. authed === false → LoginPage/SignupPage 표시
   ↓
5. 로그인 성공 → setAuthed(true)
   ↓
6. authed === true → BrowserRouter 활성화
```

### API 호출 흐름 (MealPage 예시)

```
1. MealPage 마운트
   ↓
2. useMealData(currentDate) 호출
   ↓
3. GET /api/meals?date=YYYY-MM-DD
   ↓
4. meals 상태 업데이트
   ↓
5. MealList, MealStats 리렌더링
```

### 상태 관리

```
- App.jsx: authed, view (login/signup)
- Home.jsx: currentDate, dashboardData
- MealPage.jsx: currentDate, mealType, inputValue, calorieInput, editingId
- Cart: CartList 내부 상태 관리
```

## 🛠️ 주요 라이브러리

```
- react: ^19.2.0
- react-dom: ^19.2.0
- react-router-dom: ^7.11.0 (라우팅)
- react-datepicker: ^9.1.0 (날짜 선택)
- axios: ^1.13.2 (HTTP 요청)
- vite: ^7.2.2 (빌드 도구)
```

## 📝 주요 특징

1. **인증 우회**: 개발용으로 `authed` 기본값이 `true`
2. **공통 레이아웃**: Header와 Footer가 모든 페이지에 공통 적용
3. **컴포넌트 분리**: 도메인별로 폴더 구조화 (Meal, Cart, Layout)
4. **스타일 분리**: 전역 스타일과 컴포넌트별 스타일 분리
5. **API 통신**: localhost:8080/api/ 엔드포인트 사용
